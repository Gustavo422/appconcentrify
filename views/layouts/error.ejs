<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Erro - Concentrify">
    <meta name="keywords" content="erro, concentrify">
    <meta name="author" content="Concentrify">
    
    <title><%= title || 'Erro - Concentrify' %></title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/favicon.png">
    
    <!-- CSS -->
    <link href="/css/output.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Meta tags para SEO -->
    <meta property="og:title" content="<%= title || 'Erro - Concentrify' %>">
    <meta property="og:description" content="Página de erro">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.FRONTEND_URL || 'http://localhost:3000' %>">
</head>
<body class="min-h-screen bg-gray-50">
    <!-- Navbar simples -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <div class="flex items-center">
                    <a href="/products" class="flex items-center text-xl font-bold text-blue-600 hover:text-blue-700">
                        <i class="fas fa-graduation-cap mr-2"></i>
                        Concentrify
                    </a>
                </div>
                
                <!-- Links de navegação -->
                <div class="hidden md:flex items-center space-x-4">
                    <a href="/products" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-box mr-2"></i>
                        Produtos
                    </a>
                    <a href="/auth/login" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-sign-in-alt mr-2"></i>
                        Login
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Conteúdo da página de erro -->
    <%- body %>

    <!-- Footer simples -->
    <footer class="bg-white border-t border-gray-200 mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="text-center text-gray-600">
                <p>&copy; <%= new Date().getFullYear() %> Concentrify. Todos os direitos reservados.</p>
                <p class="text-sm mt-2">Plataforma educacional para concursos públicos</p>
            </div>
        </div>
    </footer>
</body>
</html> 